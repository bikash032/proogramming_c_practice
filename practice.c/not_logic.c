#include<stdio.h>

int main(){
    int a =10;
    int b =20;
    if (!(a>0 && b>0))// 10 is not > 0 and  20 is not greater then 0;
    {
        printf(" both vallues are greater then 0\n");
    }
    else
    {
        printf("both values are  smaller then 0\n");
    }
return 0;
}
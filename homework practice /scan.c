#include <stdio.h>

int main()
{
    int a;
    int b;
    printf("enter the value of a:");
    scanf("%d", &a);
    printf("enter the value of b:");
    scanf("%d", &b);
    printf("the sum of a and b is %d\n", a +b);
    printf("the sub of a and b is %d\n", a -b);
    printf("the divid of a and b is %d\n", a /b);
    printf("the multiply of a and b is %d\n", a *b);
    return 0;
}
// arthematic operators
#include <stdio.h>

int main()
{
    int a = 10;
    int b = 20;
    printf("the sum of a and b is:%d\n", a + b);      // add operation
    printf("the sub of a and b is:%d\n", a - b);      // sub operators
    printf("the multiply of a and b is:%d\n", a * b); // multiply operators
    printf("the div of a and b is:%d\n", a / b);      // divide operators
    printf("the modolus of a and b is:%d\n", b % a);  // divide operators
    printf("the unary plus  of a is:%d\n", +a);       // urinary  operators (to show the plus value )
    printf("the unary minus  of a is:%d\n", -a);      // urinary  operators (to shw the minus value

    return 0;
}